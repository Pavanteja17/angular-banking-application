import { WithdrawLimitValidatorDirective } from './withdraw-limit-validator.directive';

describe('WithdrawLimitValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new WithdrawLimitValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
